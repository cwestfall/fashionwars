<%= form_for(@contestant_entry, :html => { :multipart => true }) do |f| %>
  <% if @contestant_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contestant_entry.errors.count, "error") %> prohibited this contestant_entry from being saved:</h2>

      <ul>
      <% @contestant_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :contestant_id %><br />
    <%= select("contestant_entry", "contestant_id", Contestant.all.collect {|c| [ c.first_name, c.id ] }, {:include_blank => 'None'}) %>
  </div>
  <div class="field">
    <%= f.label :photo %><br />
    <%= f.file_field :photo %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
